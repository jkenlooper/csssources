{
  "name": "csssources",
  "version": "0.0.1",
  "description": "Use postcss-import plugin to list files imported by parsing the sourcemap.",
  "main": "index.js",
  "bin": {
    "csssources": "./bin/csssources"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jake Hickenlooper",
  "license": "WTFPL",
  "dependencies": {
    "postcss": "^5.0.8",
    "postcss-import": "^7.0.0",
    "yargs": "^3.27.0"
  }
}
